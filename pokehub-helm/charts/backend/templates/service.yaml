apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend.fullname" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "frontend.name" . }}
  ports:
    - port: 8000
      targetPort: 8000