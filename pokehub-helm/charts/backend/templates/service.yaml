apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend.fullname" . }}
spec:
  type: NodePort
  selector:
    app: {{ include "frontend.name" . }}
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
